<div>
    {{#each this}}
    <section>
        <div>
            list of dogs
            {{this.dog_name}}
            {{size}}
            {{owner.owner_name}}
            {{owner.email}}
            {{owner.phone}}
            {{owner.address}}
            {{walker.walker_name}}
            {{walker.email}}
            {{walker.phone}}
            {{walker.hourly_rate}}
        </div>
    </section>
    {{/each}}
</div>